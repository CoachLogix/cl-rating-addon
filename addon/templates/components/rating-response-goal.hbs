{{#paper-card-content}}
  <span class="md-caption">{{model.name}}</span><br>
  <span>{{model.description}}</span>
{{/paper-card-content}}

{{paper-divider}}

{{#paper-card-content}}
  <span class="md-caption">Rating</span><br>
  <div class="rating_goal-rating-container">
    {{rating-response-goal-rating engagement=engagement value=ratingValue review=ratingReview}}
  </div>

  {{yield}}
{{/paper-card-content}}
